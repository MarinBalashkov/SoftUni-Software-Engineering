newFunction();
reveerseArray();

function newFunction() {
    let array = [3, 4, 5, 6, 7];

    array.forEach(element => {
        console.log(element);
    });
}
let words = new ["a", "b", "c" ]

function reveerseArray(words) {
    for (let i = 0; i < words.length; i++) {
        const rev = words.reveerseArray;
        console.log(rev);
    }
}